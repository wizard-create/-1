# 正确的 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 先复制 package.json（注意语法：COPY 源文件 目标位置）
COPY package.json ./
COPY package-lock.json* ./

# 安装依赖
RUN npm install

# 复制所有源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "index.js"]